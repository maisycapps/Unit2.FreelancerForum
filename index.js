// STATE
class freelancer {
    constructor (name, occupation, startingPrice){
    name = this.name,
    occupation = this.occupation,
    startingPrice = this.startingPrice
    }
}

const alice = new freelancer("Alice", "writer", "30");
console.log(alice);

